name: Obtener último APK de EAS y notificar a Discord

on:
  push:
    branches:
      - test-2  # Cambiar a la rama adecuada, por ejemplo, main

jobs:
  get-latest-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Instalar dependencias
        run: |
          sudo apt-get update
          sudo apt-get install -y expect
          npm install -g eas-cli

      - name: Autenticarse con eas login
        run: |
          expect <<EOF
          spawn eas login
          expect "Email address:"
          send "${{ vars.EAS_USERNAME }}\r"
          expect "Password:"
          send "${{ vars.EAS_PASSWORD }}\r"
          expect eof
          EOF
        env:
          EAS_EMAIL: ${{ vars.EAS_EMAIL }}
          EAS_PASSWORD: ${{ vars.EAS_PASSWORD }}

      - name: Obtener última URL del APK
        id: get_apk_url
        run: |
         echo "📦 Obteniendo información del último build..."

          # Ejecutar el comando e imprimir la salida estándar y de error
          eas build:list --limit 1 --platform android --json --non-interactive > build_output.json 2> build_error.log

          # Mostrar la salida de la ejecución para depuración
          echo "🔍 Salida estándar del build:"
          cat build_output.json

          echo "🔍 Salida de error (si la hay):"
          cat build_error.log

          # Verificar si la salida está vacía o contiene errores
          if [ ! -s build_output.json ]; then
            echo "❌ Error: No se obtuvo información del build o hubo un error en el comando."
            exit 1
          fi

          # Extraer la URL del APK usando jq
          APK_URL=$(cat build_output.json | jq -r '.[0].artifacts.buildUrl')

          # Verificar si se obtuvo la URL del APK
          if [ -z "$APK_URL" ]; then
            echo "❌ No se encontró la URL del APK."
            exit 1
          fi

          echo "📦 APK URL: $APK_URL"

          # Enviar la URL a Discord
          curl -X POST -H "Content-Type: application/json" \
          -d "{\"content\": \"🚀 Nuevo APK generado: $APK_URL\"}" \
          ${{ secrets.DISCORD_WEBHOOK_URL }}