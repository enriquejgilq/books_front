name: Notify Discord on Merge to Develop

on:
  push:
    branches:
      - test-2  # Se ejecuta solo cuando haya un push a la rama 'test-2'

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send post-build message to Discord
        run: |
          DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/1359492861376073849/UG5D1MUvgsO2cPYacgJX9md7S19nGOQ0W0cOov9IN7pcfyXW1KxJXjewK_EjWZm4FmBA"
          MESSAGE="🚀 El build de la aplicación ha sido completado exitosamente."
          curl -X POST "$DISCORD_WEBHOOK_URL" -H "Content-Type: application/json" -d "{\"content\": \"$MESSAGE\"}"
